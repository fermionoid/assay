# inbox: work (EXAMPLE BRANCH, rename me)

Raw captures, append-only, no judgment. Anything goes here.
Format, one per line:

- [ISO8601] raw text

Use the capture script or just append by hand. Nothing in this file is a keep.
Keeps are made later, during `organize`, and only by writing a bet.
